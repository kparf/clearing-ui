import React from 'react';
import ReactDOM from 'react-dom';
import ProviderRegistration__Form from './provider-registration__form';


it('test provider registration', () => {
    const div = document.createElement('div');
    // ReactDOM.render(<ProviderRegistration__Form />, div);
});
